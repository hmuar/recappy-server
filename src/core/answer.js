let minQuality = 0;
let maxQuality = 5;

let Answer = {
  min: minQuality,
  max: maxQuality,
  // a way of answering as confirmation,
  // effectively considered max quality
  ok: maxQuality
}

export default Answer;
